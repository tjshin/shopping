<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.study.notice.NoticeMapper">

	<!-- 조회수 증가 -->
	<update id="upCnt" parameterType="int">
		update notice
		set cnt = cnt + 1
		where noticeno = #{noticeno}
	</update>

	<!-- 조회 -->
	<select id="read" resultType="com.study.notice.NoticeDTO">
		SELECT noticeno, title, content, wname, cnt, rdate
		FROM notice
		WHERE noticeno=#{noticeno}
	</select>

	<!-- 수정 -->
	<update id="update" parameterType="com.study.notice.NoticeDTO">
		UPDATE notice
		SET title=#{title}, content=#{content}, wname=#{wname}
		WHERE noticeno=#{noticeno}
	</update>

	<!-- 삭제 -->
	<delete id="delete" parameterType="int">
		DELETE FROM notice
		WHERE noticeno=#{noticeno}
	</delete>

	<!-- 등록 -->
	<insert id="create" parameterType="com.study.notice.NoticeDTO">
		INSERT INTO notice(noticeno, title, content, wname, cnt, rdate)
		values((select nvl(max(noticeno),0)+1 as noticeno from notice),
		#{title}, #{content}, #{wname}, 0, sysdate)
	</insert>
	<!-- 목록 -->
	<select id="list" parameterType="Map"
		resultType="com.study.notice.NoticeDTO">
		select noticeno, title, wname, cnt,
		to_char(rdate, 'yyyy-mm-dd') rdate, r
		from(
		select noticeno, title, wname, cnt, rdate, rownum r
		from(
		select noticeno, title, wname, cnt, rdate
		FROM notice
		<where>
			<choose>
				<when test="col=='wname'">
					wname like '%'||#{word}||'%'
				</when>
				<when test="col=='title'">
					title like '%'||#{word}||'%'
				</when>
				<when test="col=='content'">
					content like '%'||#{word}||'%'
				</when>
				<when test="col=='title_content'">
					title like '%'||#{word}||'%'
					or
					content like
					'%'||#{word}||'%'
				</when>
			</choose>
		</where>
		ORDER BY noticeno DESC
		)
		)
    <![CDATA[where r >= #{sno} and r <= #{eno}]]>
	</select>
	<!--total -->
	<select id="total" resultType="int" parameterType="Map">
		select count(*) from notice
		<where>
			<choose>
				<when test="col=='wname'">
					wname like '%'||#{word}||'%'
				</when>
				<when test="col=='title'">
					title like '%'||#{word}||'%'
				</when>
				<when test="col=='content'">
					content like '%'||#{word}||'%'
				</when>
				<when test="col=='title_content'">
					title like '%'||#{word}||'%'
					or
					content like
					'%'||#{word}||'%'
				</when>
			</choose>
		</where>
	</select>
</mapper>